import React, { Component } from "react";
import { Header } from "semantic-ui-react";

export class Sport extends Component {
    render () {
        return (
            <Header as="h1">Sport</Header>
        );
    }
}